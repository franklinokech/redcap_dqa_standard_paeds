# =============================================================================
# CIN Neonatal DQA - Docker Compose
# =============================================================================

services:
  dqa:
    build: .
    container_name: cin_neon_standard_dqa
    environment:
      - REDCAP_URL=${REDCAP_URL}
      - REDCAP_CIN_NEON_STANDARD_API_TOKEN=${REDCAP_CIN_NEON_STANDARD_API_TOKEN}
      - DQA_START_DATE=${DQA_START_DATE:-2025-09-08}
      - DQA_END_DATE=${DQA_END_DATE:-2026-05-27}
      - DATA_SOURCE=${DATA_SOURCE:-redcap}
    env_file:
      - .env
    volumes:
      # Mount data directories for persistence
      - ./cin_neon/scripts:/app/cin_neon/scripts
      - ./cin_neon/data/raw:/app/cin_neon/data/raw
      - ./cin_neon/data/processed:/app/cin_neon/data/processed
      - ./cin_neon/data/output:/app/cin_neon/data/output
      - ./cin_neon/logs:/app/cin_neon/logs
      - ./.env:/app/.env:ro
      # Mount common utilities
      - ./common:/app/common
    restart: "no"
    networks:
      - dqa_network
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 8G
        reservations:
          memory: 2G

networks:
  dqa_network:
    driver: bridge
