# =============================================================================
# CIN Neonatal DQA - Docker Compose
# =============================================================================

services:
  dqa:
    build: .
    container_name: cin_paeds_standard_dqa
    environment:
      - REDCAP_URL=${REDCAP_URL}
      - REDCAP_CIN_PAEDS_STANDARD_API_TOKEN=${REDCAP_CIN_PAEDS_STANDARD_API_TOKEN}
      - DQA_START_DATE=${DQA_START_DATE:-2025-09-08}
      - DQA_END_DATE=${DQA_END_DATE:-2026-05-27}
      - DATA_SOURCE=${DATA_SOURCE:-redcap}
    env_file:
      - .env
    volumes:
      # Mount data directories for persistence
      - ./cin_paeds/scripts:/app/cin_paeds/scripts
      - ./cin_paeds/data/raw:/app/cin_paeds/data/raw
      - ./cin_paeds/data/processed:/app/cin_paeds/data/processed
      - ./cin_paeds/data/output:/app/cin_paeds/data/output
      - ./cin_paeds/logs:/app/cin_paeds/logs
      - ./.env:/app/.env:ro
      # Mount common utilities
      - ./common:/app/common
    restart: "no"
    networks:
      - dqa_network
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 8G
        reservations:
          memory: 2G

networks:
  dqa_network:
    driver: bridge
